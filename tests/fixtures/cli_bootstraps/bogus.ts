/**
 * Fixture: a bogus bootstrap that does NOT default-export a ConnectorApp.
 * Used by cli.test.ts to verify the CLI rejects non-ConnectorApp default exports.
 */

export default { notAConnectorApp: true };
